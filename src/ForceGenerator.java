/**
 * simple class to set methods to implement
 */
public abstract class ForceGenerator {
    /**
     * updates the force on the physics object p
     * @param p physics object to update force on
     */
    abstract void updateForce(PhysicsObject p);
}
